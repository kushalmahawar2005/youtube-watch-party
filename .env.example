# server
PORT=3001
# YouTube Data API v3 key (server only). Enables in-room video search + video details.
YOUTUBE_API_KEY=
# Only when frontend is hosted on a different domain (e.g. Netlify). Comma separated.
CLIENT_ORIGIN=

# client (build time) - only when frontend and backend are on different domains
VITE_SERVER_URL=
